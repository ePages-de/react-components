machine:
  node:
    version: 6.9.1

test:
  post:
    - npm run build
    # Ensure build folder is up-to-date
    - git diff && exit $(git -c core.filemode=false status --porcelain | wc -l)
    - bash <(curl -s https://codecov.io/bash)
